---
swagger: "2.0"
info:
  title: "API Consulta Saldos"
  description: "Obtiene todas las cuentas con saldos de un cliente.\n<strong>Ejemplo\
    \ de uso:</strong> al listar las cuentas activas del cliente en aplicaciones mó\
    viles.\n<strong>Parametros entrada:</strong> información del cliente y/o tipo\
    \ de cuenta. \n<strong>Parametros salida:</strong> listado de cuentas con descripció\
    n, id completo, estado, tipo de firma, saldo disponible y contable."
  version: "1.0.0"
  x-ibm-name: "ms-consulta-saldos"
host: "consultasaldos-UAT.interbankclouduat.pe"
basePath: "/ms"
paths:
  /consultarsaldos:
    get:
      parameters:
      - name: "codigoUnicoCliente"
        in: "query"
        required: true
        type: "string"
        x-example: "65112400"
      - name: "numeroCuenta"
        in: "query"
        required: false
        type: "string"
        x-example: "02683919"
      responses:
        200:
          description: "Conexion exitosa / Error tipo 01: Contraseña incorrecta en\
            \ la credencial del MS./ Error tipo 02: No se ingresa un dato obligatorio\
            \ o formato incorrecto. / Error tipo 03: Cambio de Puerto del MS en el\
            \ Docker./Error tipo 04: Cuando el bus a codificado algo mal en el servicio./Error\
            \ tipo 05: No se ingresa un dato obligatorio o formato incorrecto."
          schema:
            $ref: "#/definitions/MessageResponse"
        401:
          description: "Acceso no autorizado"
        404:
          description: "Ruta no encontrada"
        500:
          description: "Error interno del servicio. Favor intente luego"
definitions:
  Cuenta:
    type: "object"
    properties:
      codigoUnicoCliente:
        type: "string"
        example: "48405934"
      producto:
        type: "string"
        example: "002"
      sProducto:
        type: "string"
        example: "205"
      moneda:
        type: "string"
        example: "001"
      tasaAnual:
        type: "string"
        example: "110.87"
      saldoDisponible:
        type: "string"
        example: "1732.0010"
      saldoContable:
        type: "string"
        example: "1732.0010"
      numeroCuenta:
        type: "string"
        example: "1234567890"
  MessageResponse:
    type: array
    items:
      $ref: "#/definitions/Cuenta"
x-ibm-configuration:
  enforced: true
  testable: true
  phase: "realized"
  cors:
    enabled: true
  assembly:
    execute:
    - proxy:
        title: "proxy"
        timeout: 60
        verb: "keep"
        cache-response: "protocol"
        cache-ttl: 900
        version: "1.0.0"
        description: "Service"
        target-url: "https://hackathon-api-server.herokuapp.com/consultarsaldos"
    catch: []
securityDefinitions:
  SecureID:
    type: "apiKey"
    description: ""
    in: "header"
    name: "X-IBM-Client-Id"
security:
- SecureID: []
